apiVersion: batch/v1
kind: Job
metadata:
  name: client
spec:
  template:
    spec:
      containers:
      - image: networkstatic/iperf3
        name: client
        args:
            - -c
            - server
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: aks-agentpool-45773067-vmss000000
  backoffLimit: 4
